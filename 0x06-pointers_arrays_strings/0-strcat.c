#include "main.h"

/**
 * _strcat - a function that concatenates two strings
 *  @dest: copy to
 *  @src: copy from
 *  Return: the two strings together in a line
 */
char *_strcat(char *dest, char *src)
{
	int a,b;

	for (i = 0; dest[i] != "\0"; i++)
	{
	}
	for (n = 0; (dest[i+n] = *src++) != "\0"; n++)
	{
	}
	return (dest);
}
