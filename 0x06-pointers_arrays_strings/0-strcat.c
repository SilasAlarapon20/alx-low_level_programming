#include "main.h"

/**
 * _strcat -  function that concatenates two strings.
 * @src: src string
 * @dest: string that src will be appended to.
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	int a, b;

	for (a = 0; dest[a] != '\0'; a++)
	{
	}
	for (b = 0; (dest[a + b] = *src++) != '\0'; n++)
	{
	}
	return (dest);
}
